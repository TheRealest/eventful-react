var React = require('react');

var bootstrapReactElement = function(reactElement) {
  // console.log('this.props.children',reactElement);
  // React.Children.forEach(reactElement.props.children,function(elem) {
  //   console.log('in foreach');
  //   console.log(elem.props);
  // });
};

module.exports = bootstrapReactElement;